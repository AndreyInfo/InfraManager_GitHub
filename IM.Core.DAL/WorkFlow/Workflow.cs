using System;

namespace InfraManager.DAL.Workflow
{
    public class Workflow
    {
        public static bool IsFinished(ObjectClass classId, Guid id) => throw new NotSupportedException();
        public static bool IsOverdue(ObjectClass classId, Guid id) => throw new NotSupportedException();
    }
}
